// ignore_for_file: prefer_const_constructors
import 'package:api_util/api_util.dart';
import 'package:test/test.dart';

void main() {
  group('ApiUtil', () {
    test('can be instantiated', () {
      expect(ApiUtils(), isNotNull);
    });
  });
}
